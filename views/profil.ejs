<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <title>Mon Profil</title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <%- include("fragments/header") %>

    <main class="profil-container">
      <h1>Mon profil</h1>
      <div class="profil-box">
        <ul>
          <li><strong>Nom d'utilisateur :</strong> <%= user.username %></li>
          <li><strong>Nom :</strong> <%= user.lastname %></li>
          <li><strong>Prénom :</strong> <%= user.firstname %></li>
          <li><strong>Email :</strong> <%= user.email %></li>
          <li>
            <strong>Rôle :</strong> <%= user.role == 1 ? "Apprenant" :
            "Formateur" %>
          </li>
        </ul>
        <form action="/users/profil/modifier-user" method="get">
          <button type="submit" class="btn">Modifier mon profil</button>
        </form>
      </div>
    </main>

    <%- include("fragments/footer") %>
  </body>
</html>
